# fnrco test project (Mostafa Helal)
- create database with name `fnrco`
- use environment file as `.env.example` (copy `.env.example` to `.env` file)
- run migrations `php artisan migrate`
- run `php artisan storage:link`
